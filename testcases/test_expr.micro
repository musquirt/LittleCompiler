PROGRAM expr
BEGIN

	INT a,b,c,d;
	FLOAT x,y,z,t;

	FUNCTION VOID main()
	BEGIN
		a := 1;
		b := 2;
		c := 10;
		d := 20;

		WRITE (a);
		WRITE (b);
		WRITE (c);
		WRITE(d);
		a := a + b;
		WRITE (a);
		b := a * c;
		WRITE (b);
		c := 0 - a + b;
		WRITE (c);
		d := 0 - d;
		WRITE (d);
		a := (a+b)*(d+c)-(a+b+c+d)/a;
		WRITE (a);
		
		a := a + 10;
		WRITE (a);
		b := b + a + 10;
		WRITE (b);
		c := 0 - 10;
		WRITE (c);
		x := 1.0;
		y := 2.0;
		z := 3.14159;
		WRITE (x);
		WRITE (z);
		WRITE (y);
		x := z/2.0;
		y := z/y;
		WRITE (x);
		WRITE (y);
		t := (x+y+z)/z;
		WRITE (t);
		t := t*t;
		WRITE (t);
		t := (t+z+t+t/2.0+z/4.0+z/5.0+z/6.0+z/7.0);
		WRITE (t);
	END	
END